# Used by worker webhooks, syncs, and local tool execution when they call the Notion API.
# A Custom Agent calling this tool in Notion gets a preauthenticated client from the platform.
NOTION_API_TOKEN=

